"use client";
import { LockKeyhole, Snowflake } from "lucide-react";
import { useRouter } from "next/navigation";
import { useState } from "react";
import { Button } from "@/components/ui/Button";
export default function AdminLoginPage() { const router = useRouter(); const [password, setPassword] = useState(""); const [error, setError] = useState(""); const [busy, setBusy] = useState(false); async function submit(event: React.FormEvent) { event.preventDefault(); setBusy(true); setError(""); const response = await fetch("/api/admin/login", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ password }) }); setBusy(false); if (!response.ok) { setError((await response.json()).error); return; } router.replace("/admin"); router.refresh(); } return <main id="conteudo" className="grid min-h-dvh place-items-center p-4"><form onSubmit={submit} className="glass w-full max-w-md rounded-[2rem] p-7"><Snowflake className="mx-auto size-12"/><h1 className="display mt-4 text-center text-3xl">Painel do evento</h1><p className="mt-2 text-center text-[#43647a]">Acesso exclusivo do responsável pela festa.</p><label htmlFor="password" className="mt-6 block font-bold">Senha administrativa</label><div className="relative mt-2"><LockKeyhole className="absolute left-4 top-1/2 size-5 -translate-y-1/2"/><input id="password" type="password" autoComplete="current-password" required value={password} onChange={event => setPassword(event.target.value)} className="h-13 w-full rounded-2xl border border-[#0d6794]/25 bg-white/75 pl-12 pr-4 text-base"/></div>{error && <p role="alert" className="mt-3 text-sm font-bold text-[#9f2d28]">{error}</p>}<Button type="submit" disabled={busy} className="mt-5 w-full">{busy ? "Entrando…" : "Entrar"}</Button></form></main>; }
