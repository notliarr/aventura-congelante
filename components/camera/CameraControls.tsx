import { ArrowLeft, Camera, SwitchCamera } from "lucide-react";
export function CameraControls({ onCapture, onSwitch, onBack, busy }: { onCapture: () => void; onSwitch: () => void; onBack: () => void; busy: boolean }) {
  return <div className="absolute inset-x-0 bottom-0 z-20 flex items-center justify-between bg-gradient-to-t from-[#001b2f]/90 to-transparent px-5 pb-[max(1.25rem,env(safe-area-inset-bottom))] pt-20 text-white"><button onClick={onBack} disabled={busy} className="grid size-12 place-items-center rounded-full bg-white/15 backdrop-blur" aria-label="Voltar às molduras"><ArrowLeft/></button><button onClick={onCapture} disabled={busy} className="grid size-[5.25rem] place-items-center rounded-full border-[6px] border-white bg-white/20 shadow-xl transition active:scale-95" aria-label="Tirar foto">{busy ? <span className="size-8 animate-spin rounded-full border-4 border-white border-t-transparent"/> : <Camera className="size-9"/>}</button><button onClick={onSwitch} disabled={busy} className="grid size-12 place-items-center rounded-full bg-white/15 backdrop-blur" aria-label="Alternar câmera"><SwitchCamera/></button></div>;
}
